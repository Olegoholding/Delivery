<Page x:Class="Delivery.Pages.DataPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Delivery.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DataPage">

    <Grid Background="{DynamicResource Accent}">
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="70"/>
            <RowDefinition/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="label" Text="Блюда" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="36" FontWeight="Bold" Margin="0,0,0,15"/>
        <Border Background="{DynamicResource Thickness}" Height="1" VerticalAlignment="Bottom"/>

        <Grid Grid.Row="1" Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Border Background="{DynamicResource Accent}" BorderBrush="{DynamicResource Thickness}" BorderThickness="1" Height="40" CornerRadius="15" Grid.ColumnSpan="2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="40"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Name="SrcBox" FontSize="20" BorderThickness="0" VerticalContentAlignment="Center" Margin="15,0,0,0"/>
                    <Border Width="1" Background="{DynamicResource Thickness}" Margin="0,8,0,8" HorizontalAlignment="Right"/>
                    <TextBlock Name="searchButton" MouseLeftButtonDown="searchButton_MouseLeftButtonDown" Cursor="Hand" Grid.Column="1" FontSize="26" Text="⌕" Foreground="{DynamicResource Thickness}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Grid>
            </Border>

            
        </Grid>

        <DataGrid x:Name="dataGrid" Grid.Row="2"/>

        <Button Name="AddButton" Style="{DynamicResource addButton}" Grid.Row="3" Content="Добавить" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,15,0" FontSize="22" FontWeight="Bold" Width="200" Height="40" Click="AddButton_Click"/>
        <Button x:Name="AddButton_Копировать" Style="{DynamicResource addButton}" Grid.Row="3" Content="Удалить" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="15,0,0,0" FontSize="22" FontWeight="Bold" Width="200" Height="40" Click="AddButton_Копировать_Click"/>

    </Grid>
</Page>
